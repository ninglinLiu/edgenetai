{
  "buildCommand": "corepack enable && corepack prepare pnpm@8.15.0 --activate && pnpm install && pnpm --filter @edgenetai/dashboard build",
  "outputDirectory": "apps/dashboard/.next",
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.0 --activate && pnpm install",
  "framework": "nextjs"
}
